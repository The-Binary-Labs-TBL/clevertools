# Rust mini example

Das kleinste Beispiel in diesem Ordner:

```bash
cargo run --example minimal
```

Das Beispiel nutzt eine Funktion aus `src/lib.rs`:

- `hello("Rust")` erzeugt einen String
- `examples/minimal.rs` gibt ihn aus
